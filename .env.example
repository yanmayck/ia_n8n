# Configuration for the database
# For SQLite, it will be a file in the same directory
DATABASE_URL="sqlite:///./chat_app.db"

# To use Supabase (PostgreSQL), comment the line above and uncomment the line below
# DATABASE_URL="postgresql://user:password@host:port/database"

# API Key for the Google Gemini model
GEMINI_API_KEY=""